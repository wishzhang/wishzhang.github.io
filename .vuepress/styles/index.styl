@import "variables.styl";
@import "overwrite.styl";

.footer {
  display: true;
}

.valine-wrapper {

  #valine {
    * {
      font-family: Ubuntu, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Cantarell, "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif;
    }

    /*编辑器公共样式*/

    .vwrap {
      background: rgba(247, 248, 250, .7) !important;

      .cancel-reply-btn {
        top: 10px;
        right: 10px;
        border: none !important;
        width: 18px;
        height: 18px;
        color: #8a919f;
      }

      .vedit {
        .vinput {
          color: #252933 !important;
        }

        > .vrow {
          display: none !important;
        }
      }

      > .vrow {
        padding-bottom: 2px !important;
        padding-right: 6px !important;

        .vcol:first-child {
          display: none;
        }

        .vcol {
          width: 100%;
        }

        .vsubmit {
          width: 80px !important;
          font-size: 14px !important;
        }
      }
    }

    /*end*/

    /*评论公共样式*/

    .vreply-wrapper {
      .vwrap {
        border: none !important;
      }
    }

    .vquote {
      background: rgba(247, 248, 250, .7) !important;

      .vcard {
        padding-top: 0px !important;
      }

      .vmeta {
        margin-bottom: 0 !important;
        padding-right: 8px !important;
      }

      .vcontent {
        padding: 8px 0 !important;
      }
    }

    /*end*/

    .vcount {
      padding: 0;
      margin-top: 48px;
    }

    .vpanel {
    }

    .vcards {
      .vcard {
        padding-top: 16px;

        .vh {
          .vhead {
            .vsys {
              display: none;
            }
          }

          .vmeta {
            margin-bottom: 8px;

            > .vtime {
              color: #8a919f;
            }

            > .vat {
              position: relative;
              top: -4px;
              margin-right: 0;
              padding: 0px 8px;
              color: #8a919f;
              border: 1px solid #efefef;
              background: white !important;
            }
          }


          .vcontent {
            margin: 0;
            padding: 8px 16px 8px;
            color: #515767 !important;
            background: rgba(247, 248, 250, .7);

            > p {
              margin: 0;
              color: #515767 !important;
            }
          }

          .vreply-wrapper {

          }
        }

      }

    }

    .vempty {
      display: none !important;
    }

    .vpower {
      display: none !important;
    }
  }
}

.sponsor-container {
  padding-right: 224px !important;
  margin-bottom: 200px !important;

  .sponsor-love {
    transform: none !important;
    left: auto !important;
  }

  .sponsor-github {
    display: none !important;
  }

  #qq-option {
    display: none;
  }

  #paypal-option {
    display: none;
  }
}

.reco-bgm-box {
  .info-box {
    font-size: 14px;

    i {
      min-width: 26px;
    }
  }
}

.reco-float-box {
  background-color: transparent !important;
}

// table
table {
  display: table;
  width: 100% !important; /*表格宽度*/
  max-width: 65em; /*表格最大宽度，避免表格过宽*/
  margin: 16px auto; /*外边距*/
  border-collapse: collapse !important; /*使用单一线条的边框*/
  empty-cells: show !important; /*单元格无内容依旧绘制边框*/

  tr {
    border: none !important;
    margin: 0;
    padding: 0;
  }

  th, td {
    height: 35px; /*统一每一行的默认高度*/
    border: 1px solid var(--c-border) !important; /*内部边框样式*/
    background: white;
    font-size: 14px;
    line-height: 1.6;
  }

  th {
    font-weight: bold; /*加粗*/
    background: var(--c-brand-light); /*背景色*/
    text-align: left !important;
    white-space: nowrap; /*表头内容强制在一行显示*/
    padding: 4px 12px; /*内边距*/
  }


  td:nth-child(1) {
    width: 120px !important;
    white-space: nowrap;
  }

  td:nth-child(2) {
    min-width: 200px !important;
  }

  td {
    padding: 8px 12px; /*内边距*/
  }
}
